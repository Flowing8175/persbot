# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# LLM Configuration
# Mode: 'internal' (run model in bot process) or 'external' (use API endpoint)
LLM_MODE=internal

# For internal mode: path to your fine-tuned model directory
MODEL_PATH=/path/to/your/model

# For external mode: OpenAI-compatible API endpoint URL (e.g., LM Studio, Ollama)
# LLM_ENDPOINT_URL=http://localhost:1234/v1
